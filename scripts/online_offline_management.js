window.addEventListener("offline", (event) => {
    // alert("Switch to offline mode");
    document.location.href = "";
});

window.addEventListener("online", (event) => {
    // alert("Switch to online mode");
    document.location.href = "";
});